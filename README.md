all projects are designed by figma, and codes produced by anima and then modified by me to display. 
commands use:
  npm ci
  npm run dev
  (for development environment)
  npm run build (for production environment)
